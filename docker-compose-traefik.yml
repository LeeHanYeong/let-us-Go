version: '3'

services:
  traefik:
    image: traefik:latest
    command: --api.insecure=true --providers.docker
    ports:
      - '80:80'
      - '8080:8080'
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
    deploy:
      placement:
        constraints:
          - node.role == manager

  caddy:
    image: caddy:latest

  django_feature:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        requirements: requirements_dev.txt
        gunicorn: /srv/.deploy/gunicorn_dev.py
        wsgi: config.wsgi.production_dev
        settings: config.settings.production_dev
    volumes:
      - static-feature:/srv/.static
    env_file:
      - .env
    labels:
      - 'traefik.http.routers.django_feature.rule=Host(`feature.localhost`)'
#
#  django_develop:
#    build:
#      context: .
#      dockerfile: Dockerfile
#      args:
#        requirements: requirements_dev.txt
#        gunicorn: /srv/.deploy/gunicorn_dev.py
#        wsgi: config.wsgi.production_dev
#        settings: config.settings.production_dev
#    volumes:
#      - static-dev:/srv/.static
#    env_file:
#      - .env
#
#  django_master:
#    build:
#      context: .
#      dockerfile: Dockerfile
#      args:
#        requirements: requirements.txt
#        gunicorn: /srv/.deploy/gunicorn_master.py
#        wsgi: config.wsgi.production_master
#        settings: config.settings.production_master
#    volumes:
#      - static-master:/srv/.static
#    env_file:
#      - .env
#
volumes:
  static-feature:
#  static-dev:
#  static-master:
